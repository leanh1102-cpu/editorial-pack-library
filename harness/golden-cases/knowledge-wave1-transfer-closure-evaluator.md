# KNOWLEDGE-WAVE1-TRANSFER-CLOSURE v0.1 — Evaluator

## Contract

STATUS: `FROZEN / EVALUATOR-ONLY`

Runner: `harness/suites/knowledge-wave1-transfer-closure-runner.md`

Score each case `2 / 1 / 0`. Maximum `4`.

Promotion closure gate:

`4/4 + hard fail 0`.

This closure supplements the original KX1 validation evidence; it does not erase the original `KV-02 = 1/2`. If this closure passes, KX1 may be treated as composite validated evidence for the specific craft-transfer weakness. If it fails, diagnose before changing KX1.

Hard fail: surface copying that preserves the prohibited source object/setting/relationship/action architecture while claiming valid transfer.

### KT-01
EXPECTED ACTION: `REVISE`.

Full credit requires a new micro-example that:
- uses no furniture;
- uses no family-home setting;
- uses no parent/child relation;
- does not center departure/return;
- uses no door/window scene furniture;
- gives the recurring object different practical functions across scenes;
- changes relational function across scenes;
- includes one character's symbolic interpretation that narration leaves unconfirmed;
- changes the symbolic proposition itself away from `the absent person is still present/will return`;
- materially changes diction and scene actions.

`1`: mechanism is preserved and several surfaces change, but one major prohibited relation/setting/symbolic architecture remains conspicuously copied.

`0`: reproduces the source surface or treats the bad imitation as acceptable. Hard fail applies.

### KT-02
EXPECTED ACTION: `REVISE`.

Full credit requires a new interaction that:
- uses no sibling relation;
- uses no door/box/container;
- uses no opening/reaching sequence;
- uses neither `đừng` nor `thôi`;
- does not simply make one phrase longer over three turns;
- preserves the mechanism that a repeated linguistic element changes social force because each recurrence meets a new pressure threshold;
- uses materially different setting, relation, actions, and wording.

`1`: functional escalation is correct but surface/action structure remains too close to the model.

`0`: copies the model/bad imitation architecture or removes the repeated-element mechanism entirely.

## Verdict

`4/4 + hard fail 0` → targeted transfer weakness closed; KX1 receives composite validation eligibility.
