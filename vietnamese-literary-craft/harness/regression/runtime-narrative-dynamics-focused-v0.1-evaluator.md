# RUNTIME NARRATIVE-DYNAMICS / AUTHOR-TASTE FOCUSED v0.1 — Evaluator

## Status
`FROZEN / RUNTIME-FOCUSED / EVALUATOR-ONLY`

Runner: `harness/regression/runtime-narrative-dynamics-focused-v0.1-runner.md`

Score each case `2 / 1 / 0`. Maximum `16`.

Runtime-focused gate:
`16/16 + HARD FAIL 0`.

This gate validates only the newly promoted runtime boundaries on the installed snapshot. It does not erase or replace earlier parent/blast-radius scores.

## Global hard fails
Any one of the following is a hard fail:
1. inventing a new event, climax mechanism, canon fact, motive, future fact, body site, contact, garment, or sensory detail not supplied;
2. deleting a quiet chapter whose supplied practical state changes carry forward;
3. treating repeated public-speech payoffs as requiring a newly invented betrayal/attack/rescue/sacrifice/document;
4. converting the unsupported exact-ledger request to `PRESERVE` merely because the text stays unchanged, or certifying its future meaning;
5. adding explanatory social abstraction when material hierarchy is already sufficient;
6. preserving unsupported whole-body sensory propagation when surrounding evidence does not independently establish it;
7. using `REVISE` for RV-08 or another sentence-local defect whose surrounding architecture is explicitly working;
8. failing to route the multi-scene/chapter cases to structural diagnosis and instead applying sentence/dialogue-only polish.

---

### RV-01
EXPECTED ACTION: `REVISE`.
Full credit requires:
- diagnose repeated scene mechanism / plateau across ordered scenes;
- use source-grounded compression, merge, removal, or reassignment so the warning survives once;
- do not invent a new encounter, complication, or alternate mechanism;
- structural action scale is appropriate because more than one scene is implicated.

### RV-02
EXPECTED ACTION: `PRESERVE`.
Full credit requires:
- recognize that brace repair, food reallocation, watch order, and unusable tool all change practical state carried forward;
- reject deletion merely because external intensity is low;
- no quota demand for confrontation/reveal/climax.

### RV-03
EXPECTED ACTION: `REVISE`.
Full credit requires:
- diagnose repeated payoff mechanism;
- do not invent an unsupported replacement climax mechanism;
- use the supplied structural options: compress/de-emphasize one payoff into aftermath and/or remove/reassign the redundant preparatory node;
- do not HOLD the whole task while source-grounded structural repair still exists.

### RV-04
EXPECTED ACTION: `HOLD-INSUFFICIENT_EVIDENCE`.
Full credit requires:
- refuse exact future meaning/motive/canon certainty;
- distinguish the observed empty line from bounded possibility and unresolved future state;
- unchanged text does not convert the task disposition to `PRESERVE`.

### RV-05
EXPECTED ACTION: `PRESERVE`.
Full credit requires:
- recognize hierarchy is already legible through placement, serving order, labor, and portion allocation;
- evidence is sufficient to judge the material functional;
- reject explanatory abstraction or prestige-style clarification.

### RV-06
EXPECTED ACTION: `MIN_EDIT`.
Full credit requires:
- remove redundant mediation/nominalization;
- remove unsupported body-wide propagation such as `len lỏi trong cơ thể`;
- preserve only dense mist + cold sensation;
- invent no body site/contact/clothing/wetness/posture/movement;
- sentence-local action scale remains `MIN_EDIT`.

### RV-07
EXPECTED ACTION: `MIN_EDIT`.
Full credit requires:
- remove redundant mediation;
- remove unsupported whole-body propagation;
- ground only in the independently supplied face contact;
- no new body response/location/ornament;
- sentence-local action scale remains `MIN_EDIT`.

### RV-08
EXPECTED ACTION: `MIN_EDIT`.
Full credit requires:
- restore the supplied cause: Nam turns back for the key;
- remove rain as causal explanation;
- change only the defective sentence;
- preserve surrounding architecture.

## Interpretation
`16/16 + HARD FAIL 0` supports `RUNTIME-FOCUSED PASS` for the current snapshot's newly promoted Narrative Dynamics / Author-Taste boundaries. Combined with static snapshot verification and already-frozen blast-radius evidence, it is sufficient for the locked roadmap's compatible-loader promotion gate when the run is executed blind against the snapshot package under test.
