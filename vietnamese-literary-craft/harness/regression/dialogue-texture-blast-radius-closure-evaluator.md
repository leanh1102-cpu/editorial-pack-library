# DIALOGUE-TEXTURE BLAST-RADIUS CLOSURE v0.1 — Evaluator

## Status
`FROZEN / EVALUATOR-ONLY`

Do not expose this file during a blind run.

## Scoring
Each case scores `2 / 1 / 0`. Maximum `4`.

Promotion closure gate:
`4/4 + hard fail 0`.

## Hard fails
1. DB-01 invents a new weather source, contact event, metaphor, or bodily consequence not supplied by the packet while claiming a minimal repair.
2. DB-01 preserves generic whole-body propagation as the final sensory mechanism.
3. DB-02 keeps rain as the cause or replaces the supplied ledger cause with another cause.
4. DB-02 performs a broad paragraph/scene rewrite instead of a local sentence repair.

### DB-01 — Sensory source preservation
Expected action: `REVISE` or `MIN_EDIT`; action label is secondary to mechanism because the source sentence contains a full mediation/propagation defect.
Full credit requires:
- remove `khiến` + `cảm thấy một luồng` mediation or equivalent redundant packaging;
- remove unsupported `lan khắp cả người` propagation;
- preserve only the established cold experience;
- add no wind/rain/snow/contact point/trembling/numbness/sweat/pain/metaphor/new body response.
Strong equivalents include restrained direct forms such as `Nàng rét buốt.` or `Nàng thấy rét buốt.` only if mediation is not reintroduced as redundant packaging.

### DB-02 — Sentence-local causal restoration
Expected action: `MIN_EDIT`.
Full credit requires:
- restore supplied cause: Lan turns back for the ledger;
- remove rain as causal explanation;
- repair only the defective sentence/local clause;
- preserve surrounding working architecture.
Strong equivalent: `Vì quay lại lấy cuốn sổ nên Lan lỡ chuyến.`

## Interpretation
A clean pass means the Dialogue Texture candidate has no demonstrated residual regression on these two narrow boundaries. It does not erase the original blast-radius scores; promotion evidence is composite.