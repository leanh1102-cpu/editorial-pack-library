# KNOWLEDGE-WAVE1-CROSS-LAYER-ACTION-CLOSURE v0.1 — Evaluator

## Contract

STATUS: `FROZEN / EVALUATOR-ONLY`

Runner: `harness/regression/knowledge-wave1-cross-layer-action-closure-runner.md`

Score each case `2 / 1 / 0`. Maximum `4`.

Promotion closure gate:
`4/4 + hard fail 0`.

This closure supplements the original cross-layer evidence; it does not erase `XR-04 = 1/2` and `XR-05 = 1/2`. If this closure passes, the demonstrated action-scale regression is closed and Task 7 may proceed to router/index promotion.

Hard fails:
- preserves or invents inaccessible POV knowledge;
- replaces the supplied causal fact with rain or another invented cause;
- rewrites broader working architecture while claiming a minimal repair.

### XA-01
EXPECTED ACTION: `MIN_EDIT`.

Full credit requires:
- remove the inaccessible note-content assertion;
- preserve strict close third through Mai;
- keep only supplied visible information or remove the defective clause;
- change only the defective sentence;
- add no new fact or knowledge channel.

`1`: fixes POV access but labels the intervention `REVISE` or rewrites beyond the defective sentence without material regression.

`0`: retains/invents access to note contents or globalizes the POV change. Hard fail applies.

### XA-02
EXPECTED ACTION: `MIN_EDIT`.

Full credit requires:
- restore the supplied cause: Nam misses the ferry because he turns back for the key;
- remove rain as the cause;
- change only the defective causal sentence;
- preserve surrounding chronology, POV, pacing, and other causal relations.

`1`: restores the correct cause but labels it `REVISE` or rewrites more surrounding material than necessary without changing the supplied causal fact.

`0`: preserves rain as cause, invents another cause, or rewrites the larger architecture unnecessarily. Hard fail applies where the supplied cause is replaced.

## Verdict

`4/4 + hard fail 0` → cross-layer action-scale regression closed; blast-radius becomes composite-pass eligible.